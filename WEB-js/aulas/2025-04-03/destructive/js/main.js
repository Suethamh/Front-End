window.addEventListener("DOMContentLoaded", () => {
    const user = {nome: "Matheus", idade:18}
    
    let vet = [10, 20, 30]

    const {nome: nomeUser, idade} = user

    console.log(nomeUser, idade)
})